# =========================================================================
# LEARNING BIOLOGY FOR LIFE - SOVEREIGN CONFIGURATION
# =========================================================================

title: "Learning Biology For Life"
subtitle: "Biology, Philosophy & Future Sciences"
name: "Md Yusuf"
email: yusuf.38bcs@gmail.com
description: "A Neural Network of Education System: Where Biology Meets Life in a Synaptic Approach."
url: "https://learningbiologyforlife.org"
baseurl: ""
logo: "/assets/images/logo.png"
favicon: "/assets/images/favicon.ico"
repository: "yusuf38bcs-oss/yusuf38bcs-oss.github.io"

# Theme
remote_theme: "mmistakes/minimal-mistakes@4.24.0"
minimal_mistakes_skin: "dark"

# Plugins
plugins:
  - jekyll-paginate
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-gist
  - jekyll-seo-tag
  - jekyll-include-cache
  - jekyll-remote-theme

# Pagination
paginate: 10
paginate_path: /page:num/

# Collections
collections:
  biology:
    output: true
    permalink: /:collection/:name/        # CLEAN: strips date prefix after rename
  synaptic-bridge:
    output: true
    permalink: /:collection/:path/        # Kept: explicit permalinks already clean
  life-philosophy:
    output: true
    permalink: /:collection/:path/        # RESTORED: preserves subdirectory structure
  life-practices:
    output: true
    permalink: /:collection/:name/        # CLEAN: strips date prefix after rename
  socratic:
    output: true
    permalink: /:collection/:name/        # CLEAN: strips date prefix after rename

# Defaults
defaults:
  # Posts
  - scope:
      path: ""
      type: "posts"
    values:
      layout: single
      author_profile: true
      sidebar:
        nav: "synaptic_nav"

  # Biology collection
  - scope:
      path: ""
      type: "biology"
    values:
      layout: single
      author_profile: true
      sidebar:
        nav: "synaptic_nav"

  # Synaptic Bridge collection
  - scope:
      path: ""
      type: "synaptic-bridge"
    values:
      layout: single
      author_profile: true
      sidebar:
        nav: "synaptic_nav"

  # Life Philosophy collection — RESTORED
  - scope:
      path: ""
      type: "life-philosophy"
    values:
      layout: single
      author_profile: true
      sidebar:
        nav: "synaptic_nav"

  # Life Practices collection
  - scope:
      path: ""
      type: "life-practices"
    values:
      layout: single
      author_profile: true
      sidebar:
        nav: "synaptic_nav"

  # Socratic collection
  - scope:
      path: ""
      type: "socratic"
    values:
      layout: single
      author_profile: true
      sidebar:
        nav: "synaptic_nav"

  # Static pages
  - scope:
      path: "_pages"
      type: "pages"
    values:
      layout: single
      author_profile: false

# Build settings
markdown: kramdown
highlighter: rouge
permalink: /:categories/:title/
timezone: Asia/Dhaka

# Exclude
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - .sass-cache
  - .jekyll-cache
  - .jekyll-metadata
  - README.md
  - LICENSE

# ============================================================================
# SYNAPTIC UI — Production Overrides (Auto-injected)
# ============================================================================

# Ensure correct baseurl for Cloudflare Pages
baseurl: ""
url: "https://yusuf38bcs-oss-github-io.pages.dev"

# Plugin whitelist for GitHub Pages compatibility
plugins:
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-gist
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-archives

# Sass load paths
sass:
  sass_dir: _sass
  style: compressed

# Exclude from build
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - .sass-cache
  - .jekyll-cache
  - .jekyll-metadata
  - README.md
  - "*.gemspec"

# Custom head injections
head_scripts:
  - /assets/js/synaptic-navigation.js
  - /assets/js/synaptic-accessibility.js

custom_css:
  - /assets/css/synaptic-overrides.css
